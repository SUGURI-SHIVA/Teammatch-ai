{
  "name": "teammatch-ai",
  "version": "1.0.0",
  "description": "AI-powered platform for forming better project teams among students",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && npm install && npx prisma generate && npx prisma db push && npm run db:seed",
    "setup:frontend": "cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
